import "@/global.css";
import { ComingSoon } from "@/components/ui/ComingSoon";

export default function SubscriptionsTab() {
  return (
    <ComingSoon
      title="Subscriptions"
      subtitle="Library of plans, renewals, and shared spend — coming together here."
    />
  );
}
